
// yeah i may as well push this but it's gonna stay liike this lol
#if 0
	// STAR NOTE: might be used to check whether the player is touching a button with their finger or not //
	// Make sure to provide a game control first! //
	INT32 x = ev->x;
	INT32 y = ev->y;
	touchconfig_t *btn = &(layout->config[gc]);
	return TS_FingerTouchesButton(x, y, btn);
#endif
